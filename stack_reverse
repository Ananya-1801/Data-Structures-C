//WAP IN C TO REVERSE THE ELEMENTS PRESENT IN A STACK

#include <stdio.h>
#include <stdlib.h>

typedef struct Stack
{
    int size;
    int top;
    int *s;
} stack;

void create(stack *st)
{
    printf("Enter the size of the stack : ");
    scanf("%d", &st->size);
    st->top = -1;
    st->s = (int *)malloc(st->size * sizeof(int));
}

void Display(stack st)
{
    for (int i = st.top; i >= 0; i--)   // ✅ fixed syntax (was `i=st.top,i>=0`)
    {
        printf("%d ", st.s[i]);
    }
    printf("\n");
}

void push(stack *st, int x)
{
    if (st->top == st->size - 1)
    {
        printf("Stack Overflow!\n");
    }
    else
    {
        st->top++;
        st->s[st->top] = x;
    }
}

int pop(stack *st)
{
    int x = -1;
    if (st->top == -1)
    {
        printf("Stack Underflow!\n");
    }
    else
    {
        x = st->s[st->top];
        st->top--;
    }
    return x;
}


void insertAtBottom(stack *st, int x)
{
    if (st->top == -1)
    {
        push(st, x);
        return;
    }
    int temp = pop(st);
    insertAtBottom(st, x);
    push(st, temp);
}


void reverse(stack *st)
{
    if (st->top == -1)
        return;

    int temp = pop(st);
    reverse(st);
    insertAtBottom(st, temp);
}

int main()
{
    stack st;
    create(&st);

    int n, val;
    printf("Enter number of elements to push: ");
    scanf("%d", &n);

    printf("*********** CREATE STACK ************\n")
    for (int i = 0; i < n; i++)
    {
        printf("Enter element %d: ", i + 1);
        scanf("%d", &val);
        push(&st, val);
    }

    printf("Original Stack: ");
    Display(st);

    reverse(&st);

    printf("Reversed Stack: ");
    Display(st);

    return 0;
}
